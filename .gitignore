__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
!.env.example
*.log
.pytest_cache/
.ruff_cache/
data/

# Private working notes: career planning, implementation plans, decision log.
# Never publish — these describe the author's experience level and job search.
docs/private/
*roadmap*.md
CLAUDE.md

# Setuptools build metadata, regenerated on every editable install.
*.egg-info/

# Evaluation reports. Regenerated on every run; the numbers that matter are
# recorded in the docs alongside the configuration that produced them.
evaluation/results/
